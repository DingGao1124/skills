interface:
  display_name: "Migrate Application to Web"
  short_description: "Inventory, track, and close web migrations"
  default_prompt: "Use $migrate-application-to-web to map source capabilities and logic chains, execute tracked vertical slices, and produce an evidence-backed final migration summary."
